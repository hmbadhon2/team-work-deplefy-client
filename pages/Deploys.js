import React from 'react';
import Deploy from '../Components/Deploy/Deploy/Deploy';

const Deploys = () => {
    return (
        <div>
            <Deploy></Deploy>
        </div>
    );
};

export default Deploys;