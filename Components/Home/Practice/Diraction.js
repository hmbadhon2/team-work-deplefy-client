import React from 'react';

const Diraction = () => {
    return (
        <div>
                    <LoadScriptzzzz googleMapsApiKey={process.env.NEXT_GOOGLE_MAP_API_KEY}>
        </div>
    );
};

export default Diraction;